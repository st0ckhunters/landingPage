---
import Button from "./button.astro";

const { url_icon, url_functions, url_plans, url_blog, url_contact_us} = Astro.props;

---

<header>
  <a href = {url_icon} class="logo-link">
      <img src="./logo_ico.svg" alt="Logo StockHunters" class="logo-ico"/>
  </a>

  <nav>
      <a href={url_functions} class="link">Funcionalidades</a>
      <a href={url_plans} class="link">Planes</a>
      <a href={url_blog} class="link">Blog</a>
      
      <Button url={url_contact_us} target="_blank" text="Contactanos"></Button>
  </nav>
</header>

<!-- <style>
  header {
    position: sticky;
    top: 0;
    left: 0;
    right: 0;
    z-index: 1000; /* Asegura que esté sobre otros elementos */
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0 60px;
    height: 70px;
    overflow: hidden;
    font-weight: 600;
    background-color: #ffffffb1;
    backdrop-filter: blur(5px);
    box-shadow: 0 6px 8px rgba(0, 0, 0, 0.1);
    transition: padding 0.3s ease-in-out;
  }

  .logo-link {
    display: flex;
    align-items: center;
    height: 100%;
    text-decoration: none;
  }

  .logo {
    height: 100%;
    width: auto;
    display: block;
  }
  
  nav ul li {  
    display: flex;
    align-items: center;
  }

  nav ul {
    list-style: none;
    display: flex;
    gap: 30px;
    margin: 0;
    padding: 0;
  }

  nav a {
    text-decoration: none;
    color: inherit;
  }

  nav a:hover {
    color: #1A73E8;

  }
  @media screen and (max-width: 1200px) {
    header {
      padding: 0 30px;
    }
    
  }
  @media screen and (max-width: 800px) {
    nav ul li {
      visibility: hidden;
      display: none;
    }
  }
  @media screen and (max-width: 600px) {

    .logo {
      height: 80%;
    }
  }

</style> -->

<style>
  header{
    background-color: #ffffffb1;
    position: sticky;
    display: flex;
    justify-content: space-between;
    width: 100%;
    height: 100px;
    align-items: center;
    top: 0;

    backdrop-filter: blur(5px);
    box-shadow: 0 6px 8px rgba(0, 0, 0, 0.07);
  }

  .link{
    padding: 0 15px;
    cursor: pointer;
    text-decoration: none;
    color: var(--primary-text-color);
  }
  .link:hover{
    color: var(--primary-color);
  }
  nav{
    padding-right: 50px;
    transition: padding-right 0.3s ease-in-out;
  }

  .logo-ico{
    padding-left: 150px;
    width: 60px;
    transition: padding-left 0.3s ease-in-out;
  }


  /* Resposive */
  @media screen and (max-width: 1200px) {
    .logo-ico{
      padding-left: 100px;
    }
    nav{
      padding-right: 25px;
    }

  }

  @media screen and (max-width: 800px) {
    .logo-ico{
      padding-left: 80px;
    }
    .link{
      display: none;
    }
  }

  @media screen and (max-width: 600px) {
    .logo-ico{
      padding-left: 40px;
    }

  }
</style>